# Global infrastructure secrets

# Postgres — point to your existing instance, or use 'make up-all' to start a local container
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=inotives
POSTGRES_PASSWORD=changeme
POSTGRES_DB=inotives

# Platform schema name (isolates this project's tables within the shared DB)
PLATFORM_SCHEMA=openvaia
